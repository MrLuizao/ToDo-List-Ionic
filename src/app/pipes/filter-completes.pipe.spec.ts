import { FilterCompletesPipe } from './filter-completes.pipe';

describe('FilterCompletesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCompletesPipe();
    expect(pipe).toBeTruthy();
  });
});
